# Deney verisi — asla commit edilmez
data/
*.npy
*.npz
*.safetensors

# ...ama `results/` commit EDİLİR. `results/axis/assistant_axis.npy` Plan
# 3-4'ün girdisidir ve onu üreten kararla (`criterion_a.json`) aynı commit'te
# durmalı; global `*.npy` kuralı bunu sessizce dışarıda bırakıyordu.
!results/**/*.npy

# Sırlar
.env
*.key

# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/
.ipynb_checkpoints/

# Kaynak makale (4.2 MB, arXiv'den alınabilir)
*.pdf
.superpowers/
